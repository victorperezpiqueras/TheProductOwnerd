export function formatRoles(rol: string) {
  if (rol === 'productOwner') return 'Product Owner';
  else if (rol === 'stakeholder') return 'Stakeholder';
  else if (rol === 'desarrollador') return 'Developer';
}
